name: 'Gabor Test'
description: 'we dont need this'
inputs:
  issue_comment_body:
    required: true
  issue_number:
     required: true
  issue_user:
      required: true
outputs:
  issue_comment_reply:
    description: 'Reply to the request'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.issue_comment_body }}
    - ${{ inputs.issue_number }}
    - ${{ inputs.issue_user }}
